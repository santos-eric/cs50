#include <stdio.h>

int main(void)
{
    char *s = "Hi!";
    printf("%s/n", s)
}

// type definition, char * (for data address in memory), name of variable always points to the first char

typedef char *string